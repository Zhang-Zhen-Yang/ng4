import { MyunlessDirective } from './myunless.directive';

describe('MyunlessDirective', () => {
  it('should create an instance', () => {
    const directive = new MyunlessDirective();
    expect(directive).toBeTruthy();
  });
});
